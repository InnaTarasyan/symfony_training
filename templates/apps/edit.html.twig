{% extends 'base.html.twig' %}

{% block title %}Edit Apps{% endblock %}

{% block body %}
    <h1>Edit Apps</h1>

    {{ include('apps/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('apps_index') }}">back to list</a>

    {{ include('apps/_delete_form.html.twig') }}
{% endblock %}